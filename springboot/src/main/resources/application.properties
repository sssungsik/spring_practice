spring.application.name=springboot
# application.properties : springboot 프로젝트의 전반에 관련된 설정 파일
# (server, application, 라이브러리 설정)

# server.xml connection port 설정
server.port=80

# thymeleaf cache 설정 false (새로고침시 적용)
spring.thymeleaf.cache=false
